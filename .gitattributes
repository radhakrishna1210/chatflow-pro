# Shell scripts must keep LF endings: a CRLF in the shebang makes Linux fail
# with "/usr/bin/env bash^M: bad interpreter". Development happens on Windows,
# where core.autocrlf would otherwise convert them.
*.sh text eol=lf
